[project]
name = "butterfly-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "litellm",
    "python-dotenv",
    "pydantic-ai>=0.6.2",
    "nest-asyncio>=1.6.0",
]
